App Overview: A user directory app built with Kotlin.
Core functionalities: The app fetches data from the API using Retrofit, converts it using gson, and saves it into a Room database. The UI observes Room via Flow/StateFlow.

Screenshots
