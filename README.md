App Overview: A user directory app built with Kotlin.
Core functionalities: The app fetches JSON data from the API using Retrofit, converts it using Gson, and saves it into a Room database. The UI observes Room via Flow/StateFlow and tries to fetch data from the API.

Screenshots
